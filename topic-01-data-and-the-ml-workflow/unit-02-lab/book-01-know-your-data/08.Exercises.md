# Section 8: Exercises

These build the judgement the assignment rewards. They are **not** the assignment, and none can be pasted into it: the assignment uses a *different*, unfamiliar dataset and asks for your own profile, pipeline and written argument. Work top to bottom; the scaffolds leave the thinking to you.

## Exercise 8.1: Complete the profile

Finish the data dictionary for all 25 columns, and write the datasheet in full. This is the graded deliverable, so treat the table as prose: a reader who has never seen this file should be able to use it safely afterwards.

## Exercise 8.2: Investigate the `PAY_` codes properly

You inferred in Section 1 that −2 and 0 mean something like "no delay". Test that inference against other evidence.

```python
# exercise_8_2.py
# your implementation:
# - for clients with PAY_0 == -2, what do BILL_AMT1 and PAY_AMT1 look like?
# - and for PAY_0 == 0? do the two codes behave the same as each other?
# - does your -2 hypothesis survive? write two sentences either way
```

## Exercise 8.3: Build a feature from the six bill columns

Section 3.2 showed the bill columns are near-duplicates. Build one feature that captures something they don't individually (a trend, a ratio to `LIMIT_BAL`, a volatility), and check its correlation with `default` against the 0.325 that `PAY_0` achieves.

```python
# exercise_8_3.py
# your implementation:
# - construct utilisation = BILL_AMT1 / LIMIT_BAL, or a slope across the six months
# - correlate it with the target
# - did you beat 0.325? if not, is the feature still worth keeping? say why
```

## Exercise 8.4: Argue the missingness case (stretch)

Rows with a missing `EDUCATION` code default at 7.5% against a base rate of 22.1%. Investigate *why*: profile those 345 clients against the rest on `LIMIT_BAL`, `AGE` and `PAY_0`. Then write a paragraph proposing what process could have produced both the missing code and the low default rate. You will not be able to prove it; say so, and say what evidence would settle it.

## Exercise 8.5: A dataset you can't fix

Suppose you discover a dataset's positive labels were assigned by one overworked reviewer over a single week, and the negatives by an automated rule. No amount of imputation, scaling or augmentation addresses this. In half a page, no code, explain what you'd write in the datasheet and what you'd tell a manager who wants a model by Friday.

## Assignment Connection

Your Weeks 1–5 assignment hands you an unfamiliar public dataset and asks you to take it through one honest pipeline. The Week 1 slice of that (Mini-Deliverable 1A, part 1) asks you to:

- **Profile** the dataset (types, distributions, missingness, outliers): Sections 1–4.
- **Build a leakage-safe preprocessing pipeline**: Section 6, which is the object every later week builds on.
- **Augment the training split if a class is scarce**: Section 5.
- **Document** the data with a dictionary and a datasheet: Section 7.

Here's the **leap** you still have to make yourself, and that this lab deliberately does *not* do for you:

- The lab tells you where to look; the assignment gives you a dataset nobody has audited and expects you to **find** its problems unprompted. Nothing will announce that a column's codes are undocumented.
- The lab hands you the finding; the assignment asks you to **judge** what it means for your problem and to defend the choice you made about it. Recoding, flagging, imputing and leaving-alone are all defensible; *silently* doing any of them is not.
- The lab's dictionary is half-written; the assignment's must be written from nothing, and it is marked on whether the notes prove you actually looked.

Practise the moves here until they're automatic. The marks are for the judgement you build on top of them.

---
