# Section 7: Exercises

These build the judgement the assignment rewards. They are **not** the assignment, and none can be pasted into it: the assignment uses a *different*, unfamiliar dataset and asks for your own integrated pipeline and written argument. Work top to bottom; the scaffolds leave the thinking to you.

## Exercise 7.1: Beat the baseline honestly

Take the classification task and try to raise **recall on defaulters above 0.5 while keeping precision usable**, by combining a stronger model with a tuned threshold. Report what you tried and what it cost.

```python
# exercise_7_1.py
# your implementation:
# - pick a model from Section 3 (or tune one)
# - sweep the threshold as in 5.4
# - report the recall/precision you land on, and argue whether the trade is worth it
```

## Exercise 7.2: Make accuracy lie, in writing

Using the numbers you already have, write a short paragraph aimed at a manager who only wants to hear the accuracy figure, explaining why "81% accurate" is misleading them about this model's usefulness. Use the lazy-baseline number and the recall number to make your case.

## Exercise 7.3: Cost-sensitive threshold (stretch)

Suppose a false negative (a missed defaulter) costs the bank 5 times what a false positive (a wrongly flagged good customer) costs. Find the threshold that minimises total cost.

```python
# exercise_7_3.py
# your implementation:
# for t in a range of thresholds:
#     pred = (pp >= t)
#     fn = ...   # count false negatives
#     fp = ...   # count false positives
#     cost = 5 * fn + 1 * fp
# find and report the threshold with the lowest cost; compare it to 0.5
```

## Exercise 7.4: Diagnose a described curve

A classmate reports a learning curve where training AUC is 0.70 and validation AUC is 0.69, both flat, both refusing to rise with more data. They plan to gather 50,000 more rows. In a short paragraph, explain in bias–variance terms why that won't help, and say what you'd try instead.

## Exercise 7.5: Framing a fresh problem

Pick one of these and walk the full question → target → metric → threshold chain in half a page, no code:

- A hospital predicting which patients will be readmitted within 30 days.
- A streaming service predicting which subscribers will cancel next month.

## Assignment Connection

Your Weeks 1–5 assignment hands you an unfamiliar public dataset and asks you to take it through one honest pipeline. The Week 2 slice of that (Mini-Deliverable 1A, part 2) asks you to:

- **Frame** the task as regression or classification and justify the target — you practised this in Sections 1 and 6.
- **Train a baseline and a stronger model, and diagnose over/underfitting with learning curves** — Sections 3 and 4.
- **Produce a visual evaluation report with the metrics that matter, and argue explicitly where headline accuracy misleads** — Section 5, which is the beating heart of both this lab and that requirement.

Here's the **leap** you still have to make yourself, and that this lab deliberately does *not* do for you:

- The lab runs each technique in isolation on data you already know; the assignment asks you to **integrate** EDA, modelling and (later) the fairness audit into one coherent pipeline on a dataset you've never seen.
- The lab hands you the metric to compute; the assignment asks you to **choose and justify** the metric that matters for *your* problem, and to *argue* where accuracy would mislead, in your own words, about your own results.
- The lab tunes a threshold as an exercise; the assignment asks you to **defend** your threshold against the real costs in your scenario.

Practise the moves here until they're automatic. The marks are for the argument you build on top of them.

---
